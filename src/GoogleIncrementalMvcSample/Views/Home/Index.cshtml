@model GoogleIncrementalMvcSample.Models.CoursesModel

@{
    ViewData["Title"] = "Home Page";
}

<h3>Google API .NET Client - Incremental Auth Sample</h3>

<table class="table">
    <thead>
        <tr>
            <th>Action</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        <tr>
            @if (!string.IsNullOrEmpty(ViewData["PersonName"]?.ToString()))
            {
                <td>
                    <a href='@Url.Action("SignOut")'>Sign out of this site</a>
                </td>
                <td>
                    <span>Signed in as @ViewData["PersonName"] (@ViewData["PersonEmail"])</span>
                </td>
            }
            else
            {
                <td>
                    <a href='@Url.Action("SignIn")'>Get user's name and email address</a>
                </td>
                <td>
                    <span>Signed out</span>
                </td>
            }
        </tr>
        <tr>
            <td>
                <a href='@Url.Action("ListCourses")'>Get a list of Google Classroom classes</a>
            </td>
            <td>
                @if (Model?.Courses != null)
                {
                    <p>@ViewData["PersonName"]'s classes:</p>
                    <ol>
                        @foreach (var course in Model.Courses)
                        {
                            <li>@course.CourseName</li>
                        }
                    </ol>
                }
            </td>
        </tr>
    </tbody>
</table>